<!DOCTYPE html>
<html lang="en">

<head>
  <%- include('../partials/head'); %>
</head>

<body>
  <header>
    <%- include('../partials/header'); %>
  </header>
  <div class="containter">
    <% let i=0 %>
    <% show.forEach((show)=>  {  %>
    <% if ( show.url == test) { %>
     <% if ( i==0) { %>
    <div class="row">
      <div class="col">
        <% } %>
        <h6> <%= show.title %> </h6>
        <video controls class="ratio ratio-16x9" id="videoPlayer" height="320" width="565" preload="none">
          <source src="<%= show.link %> " type="video/mp4" />
          <track label="English" kind="descriptions" default> </video>
          <% if (i <=3) { i +=1 %>
            <% } %>
            <% if (i < 3) { %>
          </div>
          <div class="col">
            <% } %>
            <% if (i==3) { i=0 %>
          </div>
        </div>
    <% } %>
    <% } %>
    <% }) %>
  </div>
  </div>
</div>
  <footer>
    <%- include('../partials/footer'); %>
  </footer>
</body>

</html>